import styled from "styled-components";

export const Header = styled.section`
background-color: ${({theme})=> theme.bg};
    min-height: 90vh;
`

export const AllMenu = styled.section`
background-color: ${({theme})=> theme.bgBlue};
    min-height: 90vh;
`

export const Skill = styled.section`
background-color: ${({theme})=> theme.bg};
    min-height: 90vh;
`

export const Problem = styled.section`
background-color: ${({theme})=> theme.bgBlue};
    min-height: 90vh;
`

export const Contact = styled.section`
background-color: ${({theme})=> theme.bg};
    min-height: 90vh;
`